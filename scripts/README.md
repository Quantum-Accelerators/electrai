You should provide the path to a `root` directory containing all structures. Each structure must be stored in its own subdirectory.

1. An `coord.xyz` file containing atomic arrangement along with a `cell.dat` file. This file can include a 3x3 lattice matrix and or the lattice parameters(a,b,c,alpha,beta,gamma)
2. A `POSCAR` file
